# تحديثات صفحة إدارة المستخدمين

## التحديثات الجديدة

### 1. إضافة عمود الصورة الشخصية
- تم إضافة عمود `avatar_url` في جدول `users` في Supabase
- تم تحديث model `AppUser` لدعم الصورة الشخصية

### 2. عمليات CRUD الكاملة

#### إنشاء (Create)
- إنشاء مستخدم جديد مع جميع البيانات
- رفع صورة شخصية اختيارية

#### قراءة (Read)
- عرض جميع المستخدمين مع صورهم
- عرض تفاصيل كل مستخدم

#### تحديث (Update)
- **تعديل الصورة الشخصية**: اختيار صورة جديدة من المعرض أو حذف الصورة الحالية
- **تعديل الاسم**: تغيير اسم المستخدم
- **تعديل كلمة المرور**: تغيير كلمة المرور الأساسية
- **تعديل كلمة المرور الثانية**: تغيير الـ secpass
- **تعديل الصلاحية**: تغيير دور المستخدم (مشرف/مساعد)
- **تعديل شامل**: تعديل جميع البيانات دفعة واحدة

#### حذف (Delete)
- حذف المستخدم من النظام (ما عدا المالك)

### 3. التصميم الجديد

#### الميزات البصرية
- تصميم عصري بألوان متناسقة
- أنيميشن سلس عند عرض البطاقات
- تمييز بصري للمالك عن باقي المستخدمين
- أيقونات واضحة لكل عملية

#### واجهة المستخدم
- عرض الصورة الشخصية بشكل دائري مع إطار ملون
- أزرار سريعة لكل عملية تعديل
- نافذة منبثقة لاختيار الصورة
- نوافذ حوار لتأكيد العمليات الحساسة

### 4. التكامل مع Supabase

#### Storage
- استخدام bucket `images` لتخزين الصور
- رفع الصور في مجلد `user_avatars`
- ضغط الصور تلقائياً (800x800، جودة 85%)

#### Database
- تحديث جدول `users` مع عمود `avatar_url`
- استخدام Supabase Auth Admin API لتحديث كلمات المرور

### 5. الأمان
- التحقق من صلاحيات المستخدم قبل الوصول للصفحة
- منع حذف المالك
- تشفير كلمات المرور عبر Supabase Auth

## كيفية الاستخدام

### تعديل الصورة
1. اضغط على الصورة الشخصية للمستخدم
2. اختر "اختيار من المعرض" لرفع صورة جديدة
3. أو اختر "حذف الصورة" لإزالة الصورة الحالية

### تعديل البيانات
1. اضغط على زر "تعديل" في بطاقة المستخدم
2. اختر العملية المطلوبة:
   - تعديل الاسم
   - تعديل كلمة المرور
   - تعديل كلمة المرور الثانية
   - تعديل شامل (لتعديل كل شيء دفعة واحدة)

### حذف مستخدم
1. اضغط على أيقونة الحذف (🗑️)
2. أكد عملية الحذف

## الملفات المعدلة

1. `lib/pages/user_management_page.dart` - الصفحة الرئيسية (تم إعادة كتابتها بالكامل)
2. `lib/models/user.dart` - إضافة حقل `avatar_url`
3. Migration في Supabase - إضافة عمود `avatar_url`

## المتطلبات

- `image_picker` package (للصور)
- `flutter_staggered_animations` (للأنيميشن)
- `fluttertoast` (للإشعارات)
- Supabase Storage bucket اسمه `images`

## ملاحظات

- جميع العمليات متزامنة مع Supabase
- الصور يتم رفعها على Supabase Storage
- التصميم responsive ويعمل على جميع الأحجام
- الأنيميشن سلس ومريح للعين
