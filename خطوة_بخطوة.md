# 📱 دليل Shorebird - خطوة بخطوة

## 🎯 الهدف
تحديثات تلقائية للتطبيق بدون إرسال APK كامل على الواتساب!

---

## ✅ الوضع الحالي

### ما تم إنجازه:
- ✅ الكود كامل (100%)
- ✅ GitHub Actions جاهز
- ✅ UI Components جاهزة
- ✅ التوثيق كامل

### ما تحتاجه:
- ⏳ إضافة Token واحد فقط!

---

## 🚀 الخطوات (5 دقائق)

### الخطوة 1: احصل على Token

افتح PowerShell واكتب:

```powershell
shorebird login:ci
```

**النتيجة**: سيظهر لك token طويل - انسخه!

**مثال**:
```
Your CI token is: shorebird_v1_abc123xyz...
```

---

### الخطوة 2: أضف Token لـ GitHub

1. **افتح الرابط**:
   ```
   https://github.com/Phone-star-sketch/phone/settings/secrets/actions
   ```

2. **اضغط الزر الأخضر**: "New repository secret"

3. **املأ البيانات**:
   - **Name**: اكتب بالضبط: `SHOREBIRD_TOKEN`
   - **Value**: الصق الـ token اللي نسخته

4. **اضغط**: "Add secret"

✅ **تم!** GitHub الآن يعرف Token

---

### الخطوة 3: ارفع أول Release

افتح PowerShell في مجلد المشروع:

```powershell
# 1. احفظ كل شيء
git add .
git commit -m "Ready for Shorebird"
git push

# 2. أنشئ tag للإصدار
git tag v0.1.6
git push origin v0.1.6
```

---

### الخطوة 4: راقب البناء

1. **افتح الرابط**:
   ```
   https://github.com/Phone-star-sketch/phone/actions
   ```

2. **شوف الـ workflow**: "Shorebird Release"

3. **انتظر** (5-7 دقائق)

4. **النتيجة**: ✅ يجب أن يكتمل بنجاح

---

### الخطوة 5: حمّل APK

1. **من نفس الصفحة**: اضغط على الـ workflow اللي اكتمل

2. **نزّل**: "app-release" artifact

3. **ارسل APK للعميل** (مرة واحدة فقط!)

---

## 🎉 بعد كده؟

### للتحديثات الصغيرة:

```powershell
# 1. عدّل أي كود Dart
# مثال: غيّر لون زرار، أصلح bug، إلخ

# 2. احفظ وارفع
git add .
git commit -m "Fix: تحديث صغير"
git push

# 3. أنشئ patch
git tag patch-001
git push origin patch-001

# 4. خلاص! التحديث هيوصل للعميل تلقائياً
```

---

## 📱 تجربة المستخدم

### المستخدم هيشوف إيه؟

1. **يفتح التطبيق**
   ```
   ┌─────────────────────────┐
   │   تحديث متاح 🎉        │
   │                         │
   │  حجم صغير: 2 MB        │
   │                         │
   │  [تحديث الآن] [لاحقاً] │
   └─────────────────────────┘
   ```

2. **يضغط "تحديث الآن"**
   ```
   ┌─────────────────────────┐
   │   جاري التحميل...      │
   │   ████████░░░░ 75%      │
   └─────────────────────────┘
   ```

3. **يغلق ويفتح التطبيق**
   ```
   ✅ التحديث تم تطبيقه!
   ```

---

## 📊 المقارنة

### قبل Shorebird:

```
أنت → APK (100 MB) → واتساب → العميل
      ⏰ 10 دقائق    ⏰ 5 دقائق   ⏰ تثبيت يدوي
```

**الوقت الكلي**: 30+ دقيقة

### بعد Shorebird:

```
أنت → git push → GitHub Actions → التطبيق
      ⏰ 10 ثواني   ⏰ 5 دقائق    ⏰ تلقائي
```

**الوقت الكلي**: 5 دقائق + **صفر جهد من العميل**!

---

## 🎯 ملخص الأوامر

### أول مرة (Full Release):
```powershell
git tag v0.1.6
git push origin v0.1.6
```

### التحديثات (Patches):
```powershell
git tag patch-001
git push origin patch-001
```

### التحديثات الكبيرة (New Version):
```powershell
git tag v0.2.0
git push origin v0.2.0
```

---

## ❓ أسئلة شائعة

### س: هل أحتاج تثبيت Shorebird على جهازي؟
**ج**: لا! فقط احصل على Token مرة واحدة، وبعدها GitHub Actions يعمل كل شيء.

### س: كم التكلفة؟
**ج**: **مجاني تماماً!** 5,000 تحديث/شهر.

### س: هل يعمل مع أي تعديل؟
**ج**: 
- ✅ تعديلات Dart (UI, logic, colors, text) → Patch
- ❌ تعديلات native (permissions, packages) → Full Release

### س: كيف أعرف إذا التحديث وصل؟
**ج**: من Shorebird Console: https://console.shorebird.dev

---

## 🆘 المساعدة

### إذا واجهت مشكلة:

1. **تحقق من Token**: هل أضفته صح في GitHub Secrets؟
2. **شوف الـ logs**: في GitHub Actions
3. **اقرأ التوثيق**: `NEXT_STEPS.md`

---

## 🎉 النتيجة النهائية

**بعد 5 دقائق فقط:**

✅ التطبيق يحدّث نفسه تلقائياً  
✅ المستخدم يضغط زر واحد فقط  
✅ حجم صغير (1-5 MB)  
✅ سريع جداً  
✅ مجاني تماماً  

**مبروك! 🎊**

---

**ابدأ الآن**: `NEXT_STEPS.md`
