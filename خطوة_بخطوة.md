# 🎯 دليل سريع: كيف تستخدم Shorebird

## 📌 الخطوة الأولى: إضافة SHOREBIRD_TOKEN (مرة واحدة فقط!)

### احصل على Token:

**الطريقة 1: من الموقع (الأسهل)**
```
1. افتح: https://console.shorebird.dev
2. سجل دخول
3. اضغط على اسمك (أعلى اليمين)
4. Account Settings
5. Generate CI Token
6. انسخ الـ token
```

**الطريقة 2: من Terminal**
```powershell
shorebird login
shorebird login:ci
# انسخ الـ token اللي هيظهر
```

### أضف Token لـ GitHub:
```
1. روح: https://github.com/Phone-star-sketch/phone/settings/secrets/actions
2. اضغط: New repository secret
3. Name: SHOREBIRD_TOKEN
4. Secret: [الصق الـ token]
5. Add secret
```

---

## 🚀 الاستخدام اليومي

### 🔵 تحديث صغير (90% من الحالات)

**متى تستخدمه:**
- غيرت لون
- أصلحت bug
- عدلت text
- غيرت logic في Dart

**الخطوات:**
```powershell
# 1. عدّل الكود
# 2. احفظ التعديلات
git add .
git commit -m "Fix: إصلاح مشكلة كذا"
git push

# 3. اعمل patch
git tag patch-001
git push origin patch-001
```

**النتيجة:**
- ⏱️ 2-3 دقائق
- 📦 حجم التحديث: 1-5 MB
- 📱 العميل يفتح التطبيق → يلاقي زر "تحديث"
- ✅ يضغط عليه → التحديث يطبّق فوراً

---

### 🔴 تحديث كبير (10% من الحالات)

**متى تستخدمه:**
- أضفت permission جديدة
- غيرت في AndroidManifest.xml
- أضفت package جديد
- عدلت native code

**الخطوات:**
```powershell
# 1. عدّل الكود
# 2. احفظ التعديلات
git add .
git commit -m "Feature: إضافة ميزة جديدة"
git push

# 3. اعمل release
git tag v0.2.0
git push origin v0.2.0
```

**النتيجة:**
- ⏱️ 5-10 دقائق
- 📦 حجم التحديث: 50-100 MB
- 📱 لازم ترسل APK للعميل
- ✅ العميل يثبّت يدوياً

---

## 📊 مقارنة سريعة

| الميزة | Patch (صغير) | Release (كبير) |
|--------|-------------|---------------|
| **الحجم** | 1-5 MB | 50-100 MB |
| **الوقت** | 2-3 دقائق | 5-10 دقائق |
| **التثبيت** | تلقائي | يدوي |
| **الأمر** | `patch-XXX` | `vX.X.X` |
| **متى** | 90% من الوقت | 10% من الوقت |

---

## 🎯 أمثلة عملية

### مثال 1: غيرت لون زرار
```powershell
# عدلت الكود
git add .
git commit -m "Fix: تغيير لون الزرار"
git push
git tag patch-007
git push origin patch-007
```
✅ **النتيجة:** تحديث 2 MB - العميل يضغط "تحديث" وخلاص!

---

### مثال 2: أضفت permission للكاميرا
```powershell
# عدلت AndroidManifest.xml
git add .
git commit -m "Feature: إضافة permission الكاميرا"
git push
git tag v0.2.0
git push origin v0.2.0
```
✅ **النتيجة:** APK كامل 80 MB - لازم ترسله للعميل

---

## 🔍 كيف تتابع التحديث؟

### 1. GitHub Actions
```
https://github.com/Phone-star-sketch/phone/actions
→ شوف الـ workflow
→ لازم يكون ✅ Success
```

### 2. Shorebird Console
```
https://console.shorebird.dev
→ شوف الـ patches/releases
→ شوف كام مستخدم حمّل التحديث
```

### 3. GitHub Releases (للـ releases فقط)
```
https://github.com/Phone-star-sketch/phone/releases
→ نزّل الـ APK
→ ابعته للعميل
```

---

## ⚠️ أخطاء شائعة

### ❌ خطأ: "You must be logged in"
**السبب:** مفيش `SHOREBIRD_TOKEN` في GitHub Secrets  
**الحل:** ارجع للخطوة الأولى ⬆️

### ❌ خطأ: "No release found"
**السبب:** لازم تعمل release كامل الأول  
**الحل:** `git tag v0.1.0 && git push origin v0.1.0`

### ❌ خطأ: "Unsupported class file major version 69"
**السبب:** Java version غلط على جهازك  
**الحل:** استخدم GitHub Actions (مش محتاج تشغل محلياً)

---

## 🎉 الخلاصة

**للتحديثات اليومية:**
```powershell
git tag patch-XXX && git push origin patch-XXX
```

**للتحديثات الكبيرة:**
```powershell
git tag vX.X.X && git push origin vX.X.X
```

**وخلاص!** 🚀

---

**آخر تحديث:** 2026-05-13  
**الحالة:** ✅ جاهز للاستخدام
